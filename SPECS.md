# SPECS.md — hasher 仕様書

hasher の**外から見える振る舞い**を定義する文書。CLI の契約、データ形式、拡張属性インタフェース、端末出力を扱う。

内部実装（モジュール構成・アルゴリズム・設計判断の理由）は [ARCHITECTURE.md](./ARCHITECTURE.md) を参照。**同じ事実を両方には書かない。**

## この文書の読み方

- 各項目に `SPEC-<領域>-<3桁>` の ID を振る。ID は削除しても再利用しない。設計文書やコミットメッセージからはこの ID で名指しする。
- **本書は現行実装の振る舞いを記述したもの**であり、「あるべき仕様」ではない。実装が意図どおり動いていないと判断した箇所は ⚠️ **既知の不具合** として明示し、`ARCHITECTURE.md` の課題番号（B1〜B11）を併記する。
- 記載内容は 2026-08-23 時点の `main`（`fbb7028`）に基づき、実際にビルドして動作を確認している。

---

## 1. 概要

`SPEC-OVERVIEW-001` — **hasher とは**

hasher はファイルのハッシュ値を計算し、**その結果をファイルの拡張属性（xattr）にキャッシュする** CLI ツール。キャッシュにより、大量のファイルに対する2回目以降のハッシュ処理を、変更されたファイルだけの再計算で済ませられる。

キャッシュを土台に、次の機能を提供する。

| 用途 | サブコマンド |
| --- | --- |
| ハッシュの計算・表示 | `calc` |
| ハッシュのキャッシュ作成・更新 | `update` |
| キャッシュの確認・削除 | `show` / `clear` |
| 2ファイルの同一性判定 | `compare` |
| 2ディレクトリツリーの差分表示 | `dirdiff` |
| 重複ファイルの検出 | `duplicate` |
| 属性の有無・ハッシュ一致によるファイル検索 | `find` |
| ハッシュ一覧の TSV 出力 | `list-hash` |
| バージョン表示 | `version` |

`SPEC-OVERVIEW-002` — **対象プラットフォーム**

リリースビルドは以下 6 種。CGO は無効（`CGO_ENABLED=0`）で静的バイナリ。

| OS | アーキテクチャ | アーカイブ形式 |
| --- | --- | --- |
| `linux` | `amd64`, `arm64` | `.tar.gz` |
| `darwin` | `amd64`, `arm64` | `.tar.gz` |
| `windows` | `amd64`, `arm64` | `.zip` |

アーカイブ名は `hasher_{OS先頭大文字}_{arch}` 形式（`amd64` は `x86_64` に読み替え）。例: `hasher_Linux_x86_64.tar.gz`、`hasher_Darwin_arm64.tar.gz`、`hasher_Windows_x86_64.zip`。

> ⚠️ **既知の不具合（B10）**: `README.md` のインストール手順が示す URL は `hasher_linux_x86_64.tar.gz`（OS 名が小文字）で、実際の生成物名と一致しないため 404 になる。

`SPEC-OVERVIEW-003` — **拡張属性への依存**

キャッシュを伴う機能（`update` / `show` / `clear` / `find` / `list-hash` / `compare` / `dirdiff` / `duplicate` のディレクトリ指定）は、対象ファイルシステムが拡張属性に対応していることを前提とする。非対応の場合、属性の読み取りは「値なし」として扱われ、**毎回ハッシュが再計算される**（エラーにはならない）。属性の書き込みに失敗した場合は `SPEC-CLI-102` の警告を出して処理を続行する。

`calc` は拡張属性を読み書きしないため、この前提を必要としない。

---

## 2. 共通仕様

### `SPEC-CLI-001` — コマンド体系とグローバルフラグ

```
hasher [command] [flags] [args...]
```

`command` を省略して実行した場合はヘルプを表示して**終了コード 0**。

グローバルフラグ（全サブコマンドで受け付ける）:

| ロング | ショート | 型 | 既定値 | 説明 |
| --- | --- | --- | --- | --- |
| `--verbose` | `-v` | bool | `false` | 詳細表示（`verbose`） |
| `--recursive` | `-r` | bool | `false` | 再帰処理（`recursive`） |
| `--help` | `-h` | bool | `false` | ヘルプ表示 |

**`-r` を実際に解釈するのは `update` / `clear` / `show` の 3 つだけ**である。`dirdiff` は常に再帰、`find` / `list-hash` / `duplicate` はフラグに関係なく常に再帰的にディレクトリを辿る。`calc` / `compare` / `version` は `-r` を無視する。

`-v` を解釈するのは `update` / `clear` の 2 つ。`list-hash` は `-v` を参照せず独自の条件で詳細表示を決める（`SPEC-CLI-108`）。

上記に加え、cobra が `help` と `completion`（シェル補完スクリプト生成）の 2 コマンドを自動で提供する。

### `SPEC-CLI-002` — 終了コードの決定規則

終了コードは **0 と 1 の 2 値のみ**。ただし 1 に至る経路が 2 つあり、**画面に出る内容が異なる**。

| 経路 | 終了コード | 画面 |
| --- | --- | --- |
| サブコマンドがエラーを返した | 1 | stderr に `Error: <メッセージ>` と **Usage 全文** |
| サブコマンドが異常ステータスを返した（エラーなし） | 1 | Usage は出ない。コマンド固有のメッセージのみ |
| 正常終了 | 0 | — |

同じ「失敗」でもコマンドによってどちらの経路を通るかが違うため、画面表示は統一されていない。

**差分や重複の有無は終了コードに反映されない。** `dirdiff` は差分があっても、`duplicate` は重複があっても、`find` はヒット 0 件でも終了コード 0 を返す。差分を終了コードで判定できるのは `compare` のみ（`SPEC-CLI-103`）。

### `SPEC-CLI-003` — 引数の扱いとパス解決

- 引数として与えたパスは**そのまま**使われる。`~` の展開は行われない。相対パスは相対パスのまま扱われる。
- 例外的に `list-hash` の TSV 出力だけは絶対パスに変換する（`SPEC-FMT-001`）。
- **引数の個数を厳密に検証するのは `dirdiff`（ちょうど 2 個）のみ。** 他のサブコマンドは引数 0 個でもエラーにならず、多くは何も出力せず終了コード 0 で終わる。
- `compare` のみ実行時に「2 個未満ならエラー」を判定する。3 個目以降の引数は無視される。

---

## 3. サブコマンド

### `SPEC-CLI-101` — `calc`

```
hasher calc [-n] [FILE...]
```

ハッシュ値を計算して表示する。**拡張属性の読み書きを一切行わない**ため、キャッシュの影響を受けず、ファイルにも痕跡を残さない。

| フラグ | ショート | 型 | 既定値 | 説明 |
| --- | --- | --- | --- | --- |
| `--no-show-path` | `-n` | bool | `false` | パスを表示しない（`don't show path`） |

**標準出力** — ファイル 1 件につき 1 行。**ハッシュ値が先、パスが後**であることに注意。

```
{ハッシュ値(16進)}<TAB>{引数で与えたパス}     ← 既定
{ハッシュ値(16進)}                           ← -n 指定時
```

実行例:

```console
$ hasher calc ./a.txt
f572d396fae9206628714fb2ce00f72e94f2258f	./a.txt
$ hasher calc -n ./a.txt
f572d396fae9206628714fb2ce00f72e94f2258f
```

**スキップされる入力** — ディレクトリとシンボリックリンクは**何のメッセージも出さずに無視**される。`-r` を付けてもディレクトリを辿らない。

**標準エラー出力** — 計算に失敗した場合 `[ERROR] <メッセージ>` を出力して次のファイルへ進む。

**終了コード** — **常に 0**。個々のファイルが失敗しても 0 のまま。

### `SPEC-CLI-102` — `update`

```
hasher update [-f] [-v] [PATH...]        # ファイルを指定
hasher update -r [-f] [-v] [DIR...]      # ディレクトリを再帰処理
```

ハッシュを計算し、結果を拡張属性に保存する。既にキャッシュがあり、かつファイルが変更されていなければ再計算しない（`SPEC-XATTR-005`）。

| フラグ | ショート | 型 | 既定値 | 説明 |
| --- | --- | --- | --- | --- |
| `--force-update` | `-f` | bool | `false` | キャッシュが有効でも強制的に再計算（`Force update`） |

**`-r` の有無で受け付ける入力が入れ替わる。**

| モード | 処理対象 | 対象外の入力に対する stderr 出力 |
| --- | --- | --- |
| `-r` なし | ファイルのみ | `Skip directory : {パス}` |
| `-r` あり | ディレクトリのみ | `Not a directory, skip. : {パス}` |

`-r` ありでディレクトリが 1 件も指定されなかった場合は、何もせず終了コード 0。

**標準出力** — `-r` なしかつ `-v` 指定時のみ、1 ファイルにつき 1 行。

```
{パス}  {ハッシュ値(16進)} {マーク}
```

パスとハッシュの間は**半角スペース 2 個**、ハッシュとマークの間は 1 個。マークはハッシュを再計算した場合 `*`、キャッシュが有効で再計算しなかった場合は空文字（行末にスペースが残る）。

`-r` ありの場合の画面は進捗表示（`SPEC-OUT-003`）に従う。

**終了コード** — `-r` ありの場合、処理中にエラーがあれば 1。

> ⚠️ **既知の不具合（B1）**: `-r` **なし**のモードでは、ハッシュ更新に失敗しても**終了コード 0 が返り、エラーメッセージも出力されない**。エラー判定に使う変数が取り違えられているため、更新結果が判定に届いていない。失敗を検出したい場合、現状は `-r` ありのモードを使うか、`show` で属性を確認する必要がある。

### `SPEC-CLI-103` — `compare`

```
hasher compare FILE1 FILE2
```

2 つのファイルが同一内容かをハッシュ値で判定する。**判定結果は終了コードだけで伝えられ、標準出力・標準エラーには何も出力しない。**

フラグなし。3 個目以降の引数は無視される。

**終了コード**

| 状況 | 終了コード | 画面 |
| --- | --- | --- |
| 2 ファイルが同一 | 0 | 出力なし |
| 2 ファイルが相違 | 1 | 出力なし |
| ファイルが読めない等のエラー | 1 | 出力なし |
| 引数が 2 個未満 | 1 | `Error: too few arguments` + Usage |

**副作用** — コマンド名に反し、**両ファイルの拡張属性を更新する**（`SPEC-XATTR-005` のキャッシュ更新が走る）。読み取り専用の操作ではない。

> ⚠️ **既知の不具合（B6）**: 「内容が違う」と「ファイルが開けない・読めない」がどちらも**終了コード 1・出力なし**で返るため、スクリプトから両者を区別できない。

### `SPEC-CLI-104` — `dirdiff`

```
hasher dirdiff [-d] BASE_DIR TARGET_DIR
```

2 つのディレクトリツリーを再帰的に比較し、差分を表示する。ファイルの同一性はハッシュ値で判定するため、名前が同じでも内容が違えば差分として、名前が違っても内容が同じならリネームとして検出される。

**引数はちょうど 2 個必須**（サブコマンド中で唯一、個数が厳密に検証される）。ディレクトリでないパスを渡すと `Error: not a directory : {パス}` + Usage で終了コード 1。

| フラグ | ショート | 型 | 既定値 | 説明 |
| --- | --- | --- | --- | --- |
| `--show-only-differences` | `-d` | bool | `false` | 差分のみ表示（`Show only differences`） |

**標準出力** — ディレクトリ行とファイル行の 2 階層で出力する。

ディレクトリ行（インデントなし）:

| 状況 | 出力 | 色 |
| --- | --- | --- |
| BASE_DIR にのみ存在 | `[+] {ディレクトリパス}` | シアン |
| TARGET_DIR にのみ存在 | `[-] {ディレクトリパス}` | ピンク |
| 両方に存在し、中の全ファイルが同一 | `[=] {ディレクトリパス}` | グレー |
| 両方に存在し、差分あり | `    {ディレクトリパス}`（先頭スペース 4 個） | 無色 |

全ファイルが同一のディレクトリ行は `-d` 指定時には表示されない。

ファイル行（先頭スペース 6 個）:

```
      {マーク} {ベース名}
      [R] {ベース名}  <-->  {対応するベース名}     ← リネーム時のみ
```

マークの意味は `SPEC-OUT-002`、色は `SPEC-OUT-004` を参照。`-d` 指定時は `[=]`（同一）のファイル行を表示しない。

**終了コード** — 差分の有無に関わらず 0。比較処理自体が失敗した場合のみ、`[ERROR] dirdiff failed : {メッセージ}` を stderr に出して 1（Usage は出ない）。

**ディレクトリ自体のリネームは検出しない。** ディレクトリはパス名で突き合わせるため、名前を変えたディレクトリは「片方にのみ存在する 2 つのディレクトリ」として報告される。

> ⚠️ **既知の不具合（B11）**: **`BASE_DIR` / `TARGET_DIR` の直下に置かれたファイルは比較対象から外れ、差分があっても一切出力されない。** 比較されるのはサブディレクトリの中身だけである。
>
> ```console
> $ ls base/            # base 直下にだけ root_only.txt がある
> root_only.txt  d1/
> $ ls target/
> d1/
> $ hasher dirdiff base target
> [=] .../base/d1
>       [=] keep.txt
> $ echo $?
> 0                     # root_only.txt の追加が報告されない
> ```

### `SPEC-CLI-105` — `duplicate`

```
hasher duplicate -s (HASH_LIST_TSV|SOURCE_DIR) TARGET...
hasher duplicate -t (HASH_LIST_TSV|TARGET_DIR) SOURCE...
```

source 側の各ファイルが target 側に存在するか（＝内容が重複しているか）をハッシュ値で判定する。ディレクトリの代わりに `list-hash` が出力した TSV ファイルを指定できる。

| フラグ | ショート | 型 | 既定値 | 説明 |
| --- | --- | --- | --- | --- |
| `--source` | `-s` | string | `""` | source のハッシュファイルまたはディレクトリ |
| `--target` | `-t` | string | `""` | target のハッシュファイルまたはディレクトリ |
| `--exists-only` | `-e` | bool | `false` | 存在したものだけ表示 |
| `--missing-only` | `-m` | bool | `false` | 存在しなかったものだけ表示 |
| `--print-source-path-only` | `-f` | bool | `false` | source のパスのみ出力 |
| `--print0` | `-0` | bool | `false` | レコードを NUL 文字で区切る |

**`-s` と `-t` は同時指定できない**（`Error: can't specify both -s and -t option`）。**`-e` と `-m` も同時指定できない**（`Error: can't specify both -e and -m option`）。いずれも終了コード 1 + Usage。

**位置引数の意味は `-s` / `-t` のどちらを指定したかで入れ替わる。**

| 指定 | 位置引数の役割 |
| --- | --- |
| `-s` を指定 | 位置引数はすべて **target** |
| `-t` を指定 | 位置引数はすべて **source** |

**標準出力** — レコード区切りは既定で改行、`-0` 指定時は NUL 文字。1 レコードの書式:

```
{sourceのパス}<TAB>{一致件数}<TAB>{一致したtargetのパス}<TAB>...     ← 既定
{sourceのパス}                                                      ← -f 指定時
```

表示対象の判定:

| source | target に存在 | 既定 | `-m` | `-e` |
| --- | --- | --- | --- | --- |
| あり | あり | 表示 | 非表示 | 表示 |
| あり | なし | 表示 | 表示 | 非表示 |

色は使用しない。

**終了コード** — 重複の有無に関わらず 0。ハッシュデータの読み込みに失敗した場合のみ 1。

**注意** — `-s` も `-t` も指定しない場合、フラグの排他チェックは通過してしまい、空パスに対する stat エラーで終了コード 1 になる。

### `SPEC-CLI-106` — `find`

```
hasher find -n DIR...              # ハッシュ属性を持たないファイルを探す
hasher find -e DIR...              # ハッシュ属性を持つファイルを探す
hasher find -f SRCFILE DIR...      # SRCFILE と同じハッシュ値のファイルを探す
```

拡張属性の状態、またはハッシュ値の一致でファイルを検索する。`-r` に関係なく常に再帰的に辿る。

| フラグ | ショート | 型 | 既定値 | 説明 |
| --- | --- | --- | --- | --- |
| `--no-hash` | `-n` | bool | `false` | ハッシュ属性を持たないファイル |
| `--has-hash` | `-e` | bool | `false` | ハッシュ属性を持つファイル |
| `--file` | `-f` | string | `""` | 指定ファイルと同じハッシュ値を持つファイル |

**3 つは相互排他。** 2 つ以上を同時に指定すると、cobra が `Error: if any flags in the group [no-hash has-hash file] are set none of the others can be` を出して終了コード 1。

**どのフラグも指定しない場合は `Error: invalid argument` + Usage で終了コード 1。**

**標準出力**

| モード | 出力 |
| --- | --- |
| `-n` | ファイルパスのみ（1 行 1 件） |
| `-e` | TSV 1 行（`SPEC-FMT-001`） |
| `-f` | TSV 1 行（`SPEC-FMT-001`） |

色は使用しない。

**終了コード** — ヒット 0 件でも 0。走査に失敗した場合は 1。**引数を 1 つも与えなかった場合はエラーにならず、何も出力せず 0 を返す。**

ディレクトリ以外を位置引数に渡すと `not a directory : {パス}` エラー。`-f` の `SRCFILE` は通常ファイルである必要があり、ディレクトリなら `directory : {パス}`、シンボリックリンクなら `symboliclink : {パス}` エラーになる。

### `SPEC-CLI-107` — `clear`

```
hasher clear [PATH...]           # ファイルを指定
hasher clear -r [-v] [DIR...]    # ディレクトリを再帰処理
```

hasher が付与した拡張属性（`user.hasher` で始まるものすべて）を削除する。他のツールが付けた属性には触れない。

固有フラグなし。

- シンボリックリンクは無言でスキップ。
- ディレクトリは、`-r` なしなら**何のメッセージも出さずに**スキップする（`update` / `show` が `Skip directory` を出すのとは異なる）。`-r` ありなら再帰的に処理する。

**標準出力** — `-r` なしの場合は何も出力しない。`-r` ありの場合は進捗表示（`SPEC-OUT-003`）に従う。

**終了コード** — `-r` なしで 1 件でも削除に失敗すると 1（Usage も表示される）。`-r` ありの場合、個々のファイルの失敗はメッセージが出るだけで**終了コードに反映されず 0 を返す**。

### `SPEC-CLI-108` — `list-hash`

```
hasher list-hash [-u] [-o OUT_FILE] TARGET...
```

ファイルおよびディレクトリ配下のハッシュ一覧を TSV 形式（`SPEC-FMT-001`）で出力する。`-r` に関係なく常に再帰的に辿る。

| フラグ | ショート | 型 | 既定値 | 説明 |
| --- | --- | --- | --- | --- |
| `--out` | `-o` | string | `""` | 出力先ファイルパス |
| `--update-hash` | `-u` | bool | `false` | ハッシュが最新でない場合に更新する |

**`-u` を指定しない場合、キャッシュが無い（または古い）ファイルは出力されない。** 代わりに次の警告が stderr に出る。

```
[WARNING] The hash value has not yet been calculated. : {パス}
```

つまり `-u` なしの `list-hash` は「既にキャッシュ済みのものを一覧する」コマンドであり、`-u` を付けて初めて未計算ファイルのハッシュを計算・保存して出力する。

**出力先と進捗表示の組み合わせ** — 進捗表示の有無が `-o` と `-u` の組み合わせで決まり、**グローバルの `-v` は参照されない**。

| `-o` | `-u` | TSV の出力先 | 進捗表示 |
| --- | --- | --- | --- |
| なし | 任意 | 標準出力 | なし |
| あり | なし | 指定ファイル | なし |
| あり | あり | 指定ファイル | **あり**（標準出力に描画） |

`-o` で指定したファイルが既に存在する場合は**切り詰めて上書き**される。

**終了コード** — 出力ファイルの作成に失敗した場合は 1。個々のファイルの処理失敗はメッセージが出るだけで 0 を返す。

**ヘッダ行は出力されない。**

### `SPEC-CLI-109` — `show`

```
hasher show [PATH...]        # ファイルを指定
hasher show -r [DIR...]      # ディレクトリを再帰処理
```

hasher が付与した拡張属性の内容を TSV で表示する。固有フラグなし。`-v` は参照しない。

**標準出力** — 引数の妥当性に関わらず、**必ず最初にヘッダ行を 1 行出力する**（対象が 1 件も無くてもヘッダだけが出る）。

```
Path	HashValue	Size	ModTime	CheckedTime
```

続いて 1 ファイルにつき 1 行:

| 列 | 内容 | 対応する拡張属性 |
| --- | --- | --- |
| `Path` | ファイルパス | — |
| `HashValue` | ハッシュ値（16進） | `user.hasher.sha1` |
| `Size` | 記録時のファイルサイズ（10進バイト数） | `user.hasher.size` |
| `ModTime` | 記録時の更新時刻を **RFC3339Nano** で整形 | `user.hasher.mtime` |
| `CheckedTime` | 最終確認時刻を **RFC3339Nano** で整形 | `user.hasher.htime` |

属性が設定されていない列は**空文字**になる。時刻のパースに失敗した場合も空文字。色は使用しない。

実行例:

```console
$ hasher show ./a.txt
Path	HashValue	Size	ModTime	CheckedTime
./a.txt	f572d396...	6	2026-08-23T16:12:58.165793643+09:00	2026-08-23T16:13:08.379935+09:00
```

`-r` なしでディレクトリを渡すと stderr に `Skip directory : {パス}`。

**終了コード** — `-r` なしでファイルが開けない等のエラーがあれば 1（Usage も表示される）。`-r` ありの場合、個々のファイルのエラーは stderr に出るだけで**終了コードに反映されない**。

### `SPEC-CLI-110` — `version`

```
hasher version
```

バージョン情報を標準出力に 1 行出力する。フラグなし。**常に終了コード 0。**

```
hasher version {バージョン} {OS_アーキテクチャ} built from {リビジョン} on {ビルド日時}
```

4 つの値はリリースビルド時に埋め込まれる。埋め込みのないビルド（`go build` で直接生成した場合）では既定値が使われ、次のようになる。

```console
$ hasher version
hasher version dev unknown built from dev on unknown
```

---

## 4. データ形式

### `SPEC-FMT-001` — TSV ハッシュリスト

`list-hash` と `find -e` / `find -f` が出力し、`duplicate` が入力として受け付ける形式。**ヘッダ行は無い。** 1 行 1 ファイルで、タブ区切りの 4 列。

| 列 | 内容 | 書式 |
| --- | --- | --- |
| 1 | ファイルパス | **絶対パス**（`list-hash` の場合）。クォート・エスケープは行わない |
| 2 | ベース名 | 列 1 のファイル名部分 |
| 3 | ファイル更新時刻 | **UNIX 秒**（10進整数） |
| 4 | ハッシュ値 | `SPEC-FMT-003` の `{アルゴリズム}:{16進}` 形式 |

```
/path/to/a.txt	a.txt	1787469178	sha1:f572d396fae9206628714fb2ce00f72e94f2258f
/path/to/sub/b.txt	b.txt	1787469178	sha1:9591818c07e900db7e1e0bc4b884c945e6a61b24
```

**時刻の単位に注意**: 本形式の列 3 は **UNIX 秒**だが、拡張属性 `user.hasher.mtime`（`SPEC-XATTR-003`）は**ナノ秒**である。両者は同じ値ではない。

**パス中のタブ・改行はエスケープされない。** そのようなファイル名を含むツリーでは形式が壊れる。

読み込み時の扱い:

- `#` で始まる行はコメントとして無視される。
- 引用符は緩く解釈されるため、`"` を含むパスでもそのまま読める。
- 列が 4 個未満の行はエラーとして報告される。

### `SPEC-FMT-002` — JSON 出力

`{"path": "...", "hash": "{アルゴリズム}:{16進}"}` 形式が実装されているが、**現在どのサブコマンドからも呼ばれておらず、CLI から出力させる手段は無い。** また文字列を組み立てているだけでエスケープ処理が無いため、`"` や `\` を含むパスでは不正な JSON になる。

将来この形式を公開する場合は、エスケープの実装が前提になる。

### `SPEC-FMT-003` — ハッシュ値の表記

ハッシュ値は 2 通りの表記で現れる。

| 表記 | 例 | 使われる場所 |
| --- | --- | --- |
| 16進のみ | `f572d396fae9206628714fb2ce00f72e94f2258f` | `calc` の出力、`show` の `HashValue` 列、拡張属性の値 |
| アルゴリズム付き | `sha1:f572d396fae9206628714fb2ce00f72e94f2258f` | TSV の列 4、JSON の `hash` |

16進は**小文字**、区切り文字やプレフィックスは付かない。アルゴリズム名は `SPEC-FMT-004` の表記に従い、値との区切りは `:`。

### `SPEC-FMT-004` — ハッシュアルゴリズム

| アルゴリズム名 | 状態 |
| --- | --- |
| `sha1` | **既定。現在すべてのサブコマンドがこれを使う** |
| `sha256` | 名前としては解釈されるが、CLI から選択する手段が無い |
| `sha512` | 同上 |

**アルゴリズムを切り替える CLI フラグは存在しない。** `sha256` / `sha512` は TSV 読み込み時にアルゴリズム名として解釈されるのみで、計算に使う経路は無い。

アルゴリズム名の表記は、`SHA-1` からハイフンを除いて小文字化したもの（`sha1`）。TSV の列 4 と拡張属性名（`SPEC-XATTR-001`）の両方でこの表記を使う。

---

## 5. 拡張属性インタフェース

hasher が付与する拡張属性は、他のツールから読み取れる**公開インタフェース**として扱う。すべて `user.hasher.` で始まる。

### `SPEC-XATTR-001` — ハッシュ値

| 項目 | 内容 |
| --- | --- |
| 属性名 | `user.hasher.{アルゴリズム名}`（例: `user.hasher.sha1`） |
| 値 | ハッシュ値の16進小文字文字列。区切り・プレフィックスなし |

**属性名がアルゴリズムごとに分かれる**ため、同一ファイルに複数アルゴリズムのハッシュを併存させられる。

### `SPEC-XATTR-002` — ファイルサイズ

| 項目 | 内容 |
| --- | --- |
| 属性名 | `user.hasher.size` |
| 値 | ハッシュ計算時点のファイルサイズ（10進バイト数） |

### `SPEC-XATTR-003` — ファイル更新時刻

| 項目 | 内容 |
| --- | --- |
| 属性名 | `user.hasher.mtime` |
| 値 | ハッシュ計算時点のファイル更新時刻。**UNIX 時刻のナノ秒**を10進で表記 |

TSV 列 3（`SPEC-FMT-001`）は**秒**なので単位が異なる。

### `SPEC-XATTR-004` — 最終確認時刻

| 項目 | 内容 |
| --- | --- |
| 属性名 | `user.hasher.htime` |
| 値 | 最後にハッシュの有効性を確認した時刻。**UTC の UNIX ナノ秒**を10進で表記 |

キャッシュが有効で再計算しなかった場合も**この属性だけは更新される**。「いつ最後に検証したか」を示すもので、ハッシュ計算時刻とは限らない。

実際に付与された状態の例:

```console
$ xattr -l ./a.txt
user.hasher.htime: 1787469188379935000
user.hasher.mtime: 1787469178165793643
user.hasher.sha1: f572d396fae9206628714fb2ce00f72e94f2258f
user.hasher.size: 6
```

### `SPEC-XATTR-005` — キャッシュ有効性の判定規則

ハッシュを再計算するかどうかは、次の順で決まる。

1. `--force-update` が指定されている → **再計算**
2. `user.hasher.{アルゴリズム}` が存在しない、または読み取れない → **再計算**
3. `user.hasher.size` の値が現在のファイルサイズと**一致しない** → **再計算**
4. `user.hasher.mtime` の値が現在の更新時刻（ナノ秒）と**一致しない** → **再計算**
5. 上記のいずれにも当たらない → **キャッシュヒット。`user.hasher.htime` のみ更新**し、保存済みのハッシュ値を使う

判定に使うのは**サイズと更新時刻だけ**である。したがって「サイズと更新時刻を保ったまま内容だけを書き換えた」ファイルは、変更が検出されずキャッシュ値が返る。厳密な検証が必要な場合は `--force-update` を使う。

`user.hasher.size` と `user.hasher.mtime` は**アルゴリズムに依らず共有される**。

### `SPEC-XATTR-006` — 属性の削除範囲

`clear`（`SPEC-CLI-107`）は、属性名が **`user.hasher` で始まるものすべて**を削除する。他のツールが付与した属性（`com.apple.provenance` など）には影響しない。

---

## 6. 端末出力

### `SPEC-OUT-001` — ステータスマーク

処理結果を示すマーク。角括弧は無色で、中の文字だけに色が付く。

| マーク | 色 | 意味 |
| --- | --- | --- |
| `[OK]` | 緑 | 成功（キャッシュ有効で再計算なしを含む） |
| `[UPDATE]` | 緑 | ハッシュを更新した |
| `[ERROR]` | 赤 | エラー |
| `[FAILED]` | 赤 | 処理失敗 |
| `[WARNING]` | 黄 | 警告 |

> ⚠️ **既知の不具合（B8）**: `update -r` の更新マークだけは `[UPDATED]`（語尾が異なり、色も付かない）が使われており、`list-hash` が出す `[UPDATE]` と表記が揃っていない。
>
> 同様に、ファイル数カウント中の警告だけは `[WARN]` と表記され、他の `[WARNING]` と揃っていない。

### `SPEC-OUT-002` — 差分ステータスマーク

`dirdiff`（`SPEC-CLI-104`）が使うマーク。

| マーク | 意味 |
| --- | --- |
| `[=]` | 同一 |
| `[+]` | 追加された（BASE_DIR にのみ存在） |
| `[-]` | 削除された（TARGET_DIR にのみ存在） |
| `[>]` | 内容が異なる（BASE_DIR 側が新しい） |
| `[<]` | 内容が異なる（TARGET_DIR 側が新しい） |
| `[~]` | 内容が異なる（更新時刻は同じ） |
| `[R]` | リネームされた（内容が同一で名前が異なる） |
| `[?]` | 未判定 |

**リネーム判定は、対応候補がちょうど 1 つに定まる場合にのみ成立する。** 同じ内容のファイルが複数ある場合は曖昧さを避けて `[+]` / `[-]` として報告される。

### `SPEC-OUT-003` — 進捗表示

進捗表示が出るのは次の 3 つの場合に限られる。

| コマンド | 条件 |
| --- | --- |
| `update -r` | `-v` 指定時 |
| `clear -r` | `-v` 指定時 |
| `list-hash` | `-o` と `-u` の**両方**を指定した時（`-v` とは無関係） |

表示は**標準出力**に描画され、次の 3 要素からなる。

1. **ファイル数のカウント** — 処理開始前に `Counting files... ` （シアン）を表示し、件数を同じ位置で更新しながらカウントする。
2. **ワーカー行** — 処理中のファイルを 1 ワーカーにつき 1 行で表示する。パスが 3 階層以上ある場合は `.../{親ディレクトリ}/{ファイル名}` に短縮される。

   ```
   [Worker-0] : .../dir/file.txt [OK]
   ```

3. **進捗行** — ワーカー行の下に `{完了数} / {総数}` を表示する。総数が確定していない間は `-1` が表示されうる。

進捗表示は ANSI のカーソル制御で同じ行を書き換える方式のため、**リダイレクトやパイプに通すと表示が崩れる**（`SPEC-LIMIT-003`）。

### `SPEC-OUT-004` — 色付けの規則

**標準出力に色が付くのは `dirdiff` と進捗表示だけ。** `calc` / `list-hash` / `find` / `duplicate` / `show` のデータ行は無色なので、パイプやリダイレクトでそのまま処理できる。

`dirdiff` の色分け:

| 対象 | 色 |
| --- | --- |
| `[+]` 追加 | ライム |
| `[=]` 同一 | グレー |
| `[>]` `[<]` `[~]` 相違 | オレンジ |
| `[R]` リネーム | 黄 |
| `[-]` 削除 | ピンク |
| リネームの矢印 `<-->` | 青 |
| `[+]` ディレクトリ行 | シアン |
| `[-]` ディレクトリ行 | ピンク |

標準エラー出力のメッセージは `SPEC-OUT-001` のマークに従って着色される。

**色を無効化する手段は無い**（`SPEC-LIMIT-003`）。

### `SPEC-OUT-005` — エラーメッセージの体裁

エラーの表示形式は 3 系統が併存しており、統一されていない。

| 形式 | 使うコマンド |
| --- | --- |
| `[ERROR] {メッセージ}` | `calc` / `clear` / `dirdiff` |
| `{メッセージ}`（プレフィックスなし） | `update` / `show` |
| `Error: {メッセージ}` + Usage 全文 | エラーを返して終了したすべてのコマンド（`SPEC-CLI-002`） |

---

## 7. 受け入れている制約

現行実装が持つ制限のうち、仕様として受け入れているもの。

### `SPEC-LIMIT-001` — シンボリックリンクは処理されない

シンボリックリンクはハッシュ計算の対象にならず、辿られることもない。`calc` と `clear` は無言でスキップし、`dirdiff` は `Skip symbolic link {パス}` を表示してスキップする。

> ⚠️ **関連する不具合（B2 / B3）**: リンク判定の実装がリンクを追跡してしまうため、**この仕様は一部の経路で意図どおり機能していない**。ディレクトリへのシンボリックリンクが通常ファイルとして扱われ、処理対象に入る場合がある。詳細は [ARCHITECTURE.md](./ARCHITECTURE.md) を参照。

### `SPEC-LIMIT-002` — 隠しファイルも対象になる

ドットで始まるファイル・ディレクトリを除外する仕組みは無い。`.git` 配下や `.DS_Store` も通常のファイルとして処理される。除外したい場合は対象ディレクトリの指定で調整する必要がある。

### `SPEC-LIMIT-003` — 出力先が端末かどうかを判定しない

出力先が端末かパイプかを判定する仕組みが無く、**リダイレクトやパイプに通した場合も ANSI エスケープシーケンスがそのまま出力される。** `NO_COLOR` 環境変数にも対応しておらず、色を無効化するフラグも無い。

進捗表示（`SPEC-OUT-003`）はカーソル制御を伴うため、ファイルにリダイレクトすると特に読みにくくなる。

実務上は `SPEC-OUT-004` のとおり主要なデータ出力が無色なので、`list-hash` や `find` の結果をパイプで処理する分には問題にならない。色付きの stderr が邪魔な場合は `2>/dev/null` で捨てる。

### `SPEC-LIMIT-004` — 処理の中断・再開に対応しない

処理を安全に中断する手段は無く、`Ctrl-C` による強制終了のみ。強制終了した場合、それまでに書き込まれた拡張属性は残るため、次回実行時はその続きから再開されるかたちになる（キャッシュが有効なファイルは再計算されない）。

### `SPEC-LIMIT-005` — ハッシュ計算の並列度は 1 に固定

内部にワーカープールの仕組みはあるが、**実際に使われるワーカー数は 1 に固定**されており、並列度を変更する CLI フラグも無い。大量のファイルを処理しても複数コアは活用されない。

### `SPEC-LIMIT-006` — 引数の検証が緩い

`SPEC-CLI-003` のとおり、`dirdiff` 以外は引数の個数を検証しない。引数を渡し忘れても多くのコマンドは**何も出力せず終了コード 0 で終わる**ため、スクリプト中では成功と区別が付かない。

### `SPEC-LIMIT-007` — 対話的な操作を行わない

標準入力を読む処理は無く、確認プロンプトも表示しない。`clear` による属性の削除も確認なしで即座に実行される。すべての挙動はフラグと引数だけで決まるため、対話環境と非対話環境で動作は変わらない。
